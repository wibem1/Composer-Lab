name: Legacy comparison chat installer (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Comparison chat v27 is part of the maintained WebApp and is no longer installed by a patch workflow."
