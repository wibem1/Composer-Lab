name: Fix Validation Duplicate
on: push
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: composition-lab-v2
      - run: python3 tools/fix_validation_duplicate.py
      - run: |
          git config user.name "Composition Lab Bot"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          git commit -m "Remove duplicate technical analysis" || exit 0
          git push
