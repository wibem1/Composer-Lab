name: Legacy Composition Lab patcher (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy one-off patch pipeline is disabled. Use the maintained WebApp files directly."
